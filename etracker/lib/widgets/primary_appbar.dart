import 'package:flutter/material.dart';

class PrimaryAppBar extends StatelessWidget {
  final bool hasBackButton;

  const PrimaryAppBar({required this.hasBackButton, Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
